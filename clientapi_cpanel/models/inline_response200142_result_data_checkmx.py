# coding: utf-8

"""
    cPanel UAPI

    UAPI accesses the cPanel interface's features. Use this API to access and modify cPanel account data and settings.

    The version of the OpenAPI document: 11.125.9901.267
    Contact: cs@cpanel.net
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class InlineResponse200142ResultDataCheckmx(BaseModel):
    """
    An object containing the mail exchanger's data.
    """ # noqa: E501
    changed: Optional[StrictInt] = Field(default=None, description="Whether a change occurred during the function. * `1` - Change occurred. * `0` - **No** change.")
    detected: Optional[StrictStr] = Field(default=None, description="The mail exchanger type. * `auto` * `local` * `secondary` * `remote`")
    isprimary: Optional[StrictInt] = Field(default=None, description="Whether the mail exchanger is the primary mail exchanger. * `1` - Primary. * `0` - **Not** primary.")
    issecondary: Optional[StrictInt] = Field(default=None, description="Whether the mail exchanger is a secondary exchanger. * `1` - Secondary. * `0` - **Not** secondary.")
    local: Optional[StrictInt] = Field(default=None, description="Whether the mail exchanger is a local exchanger. * `1` - Local. * `0` - **Not** local.")
    mxcheck: Optional[StrictStr] = Field(default=None, description="The mail exchanger type. * `auto` * `local` * `secondary` * `remote`")
    remote: Optional[StrictInt] = Field(default=None, description="Whether the mail exchanger is a remote exchanger. * `1` - Remote. * `0` - **Not** remote.")
    secondary: Optional[StrictInt] = Field(default=None, description="Whether the mail exchanger is a secondary exchanger. * `1` - Secondary. * `0` - **Not** secondary.")
    warnings: Optional[List[StrictStr]] = Field(default=None, description="Warning messages, if any exist.")
    __properties: ClassVar[List[str]] = ["changed", "detected", "isprimary", "issecondary", "local", "mxcheck", "remote", "secondary", "warnings"]

    @field_validator('changed')
    def changed_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([0, 1]):
            raise ValueError("must be one of enum values (0, 1)")
        return value

    @field_validator('detected')
    def detected_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['auto', 'local', 'secondary', 'remote']):
            raise ValueError("must be one of enum values ('auto', 'local', 'secondary', 'remote')")
        return value

    @field_validator('isprimary')
    def isprimary_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([0, 1]):
            raise ValueError("must be one of enum values (0, 1)")
        return value

    @field_validator('issecondary')
    def issecondary_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([0, 1]):
            raise ValueError("must be one of enum values (0, 1)")
        return value

    @field_validator('local')
    def local_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([0, 1]):
            raise ValueError("must be one of enum values (0, 1)")
        return value

    @field_validator('mxcheck')
    def mxcheck_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['auto', 'local', 'secondary', 'remote']):
            raise ValueError("must be one of enum values ('auto', 'local', 'secondary', 'remote')")
        return value

    @field_validator('remote')
    def remote_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([0, 1]):
            raise ValueError("must be one of enum values (0, 1)")
        return value

    @field_validator('secondary')
    def secondary_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([0, 1]):
            raise ValueError("must be one of enum values (0, 1)")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InlineResponse200142ResultDataCheckmx from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InlineResponse200142ResultDataCheckmx from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "changed": obj.get("changed"),
            "detected": obj.get("detected"),
            "isprimary": obj.get("isprimary"),
            "issecondary": obj.get("issecondary"),
            "local": obj.get("local"),
            "mxcheck": obj.get("mxcheck"),
            "remote": obj.get("remote"),
            "secondary": obj.get("secondary"),
            "warnings": obj.get("warnings")
        })
        return _obj


