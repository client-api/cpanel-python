# coding: utf-8

"""
    cPanel UAPI

    UAPI accesses the cPanel interface's features. Use this API to access and modify cPanel account data and settings.

    The version of the OpenAPI document: 11.125.9901.267
    Contact: cs@cpanel.net
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class InlineResponse200328ResultData(BaseModel):
    """
    InlineResponse200328ResultData
    """ # noqa: E501
    destination: Optional[StrictStr] = Field(default=None, description="The redirect's destination URL.")
    displaydomain: Optional[StrictStr] = Field(default=None, description="The domain to redirect.  * `ALL` is the only possible value.")
    displaysourceurl: Optional[StrictStr] = Field(default=None, description="The path to the file within the domain to test, relative to the home directory.")
    docroot: Optional[StrictStr] = Field(default=None, description="The absolute file path to the source domain's document root.")
    domain: Optional[StrictStr] = Field(default=None, description="The domain to redirect.")
    kind: Optional[StrictStr] = Field(default=None, description="The kind of redirect.  * `rewrite` — The request sent a redirect to another path on the server. * `redirect` — The request sent a redirect for the URL. * `redirectmatch` — The request sent a redirect based on a regular expression match of the URL.")
    matchwww: Optional[StrictInt] = Field(default=None, description="Whether the redirect matches `www.` subdomains. * `1` — Matches. * `0` — Does not match.")
    matchwww_text: Optional[StrictStr] = Field(default=None, description="Whether the [*Redirect with or without www.* option](https://go.cpanel.net/Redirects) is active.  * `checked` — The *Redirect with or without www.* option is active.")
    opts: Optional[StrictStr] = Field(default=None, description="The options that the function passes to Apache as part of the [Rewrite rule](https://httpd.apache.org/docs/2.4/rewrite/flags.html#flag_l).")
    source: Optional[StrictStr] = Field(default=None, description="The path to the file within the domain to test, relative to the home directory.")
    sourceurl: Optional[StrictStr] = Field(default=None, description="The path to the file within the domain to test, relative to the home directory.")
    statuscode: Optional[StrictStr] = Field(default=None, description="The [HTTP Status Code](https://en.wikipedia.org/wiki/List_of_HTTP_status_codes) of the request")
    targeturl: Optional[StrictStr] = Field(default=None, description="The redirect's destination URL.")
    type: Optional[StrictStr] = Field(default=None, description="Whether the redirect is permanent or temporary.  * `permanent` — The redirect is permanent. * `temporary` — The redirect is temporary.")
    urldomain: Optional[StrictStr] = Field(default=None, description="The domain to redirect.")
    wildcard: Optional[StrictInt] = Field(default=None, description="Whether the wildcard subdomains match.  * `1` — Matches. * `0` — Does **not** match.")
    wildcard_text: Optional[StrictStr] = Field(default=None, description="Whether the [*Wild Card Redirect*](https://go.cpanel.net/Redirects) option is active.  * `checked` — The *Wild Card Redirect* option is active.")
    __properties: ClassVar[List[str]] = ["destination", "displaydomain", "displaysourceurl", "docroot", "domain", "kind", "matchwww", "matchwww_text", "opts", "source", "sourceurl", "statuscode", "targeturl", "type", "urldomain", "wildcard", "wildcard_text"]

    @field_validator('displaydomain')
    def displaydomain_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ALL']):
            raise ValueError("must be one of enum values ('ALL')")
        return value

    @field_validator('kind')
    def kind_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['rewrite', 'redirect', 'redirectmatch']):
            raise ValueError("must be one of enum values ('rewrite', 'redirect', 'redirectmatch')")
        return value

    @field_validator('matchwww')
    def matchwww_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('matchwww_text')
    def matchwww_text_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['checked']):
            raise ValueError("must be one of enum values ('checked')")
        return value

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['permanent', 'temporary']):
            raise ValueError("must be one of enum values ('permanent', 'temporary')")
        return value

    @field_validator('wildcard')
    def wildcard_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('wildcard_text')
    def wildcard_text_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['checked']):
            raise ValueError("must be one of enum values ('checked')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InlineResponse200328ResultData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InlineResponse200328ResultData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "destination": obj.get("destination"),
            "displaydomain": obj.get("displaydomain"),
            "displaysourceurl": obj.get("displaysourceurl"),
            "docroot": obj.get("docroot"),
            "domain": obj.get("domain"),
            "kind": obj.get("kind"),
            "matchwww": obj.get("matchwww"),
            "matchwww_text": obj.get("matchwww_text"),
            "opts": obj.get("opts"),
            "source": obj.get("source"),
            "sourceurl": obj.get("sourceurl"),
            "statuscode": obj.get("statuscode"),
            "targeturl": obj.get("targeturl"),
            "type": obj.get("type"),
            "urldomain": obj.get("urldomain"),
            "wildcard": obj.get("wildcard"),
            "wildcard_text": obj.get("wildcard_text")
        })
        return _obj


