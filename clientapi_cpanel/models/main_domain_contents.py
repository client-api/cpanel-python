# coding: utf-8

"""
    cPanel UAPI

    UAPI accesses the cPanel interface's features. Use this API to access and modify cPanel account data and settings.

    The version of the OpenAPI document: 11.125.9901.267
    Contact: cs@cpanel.net
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from clientapi_cpanel.models.main_domain_contents_customlog import MainDomainContentsCustomlog
from clientapi_cpanel.models.main_domain_contents_ifmodulemodsuphpc import MainDomainContentsIfmodulemodsuphpc
from clientapi_cpanel.models.main_domain_contents_scriptalias import MainDomainContentsScriptalias
from typing import Optional, Set
from typing_extensions import Self

class MainDomainContents(BaseModel):
    """
    MainDomainContents
    """ # noqa: E501
    all_aliases_valid: Optional[StrictInt] = Field(default=None, description="Whether a valid SSL certificate exists on the domain's aliases.  * `1` — A valid SSL certificate exists. * `0` — A valid SSL certificate does **not** exist.  **Note:**  This return **only** appears if you set the `return_https_redirects_status` parameter to `1`.")
    can_https_redirect: Optional[StrictInt] = Field(default=None, description="Whether a valid SSL certificate exists or AutoSSL runs on the domain.  * `1` — A valid SSL certificate exists or AutoSSL runs. * `0` — A valid SSL certificate does **not** exist and AutoSSL does **not** run.  **Note:**  This return **only** appears if you set the `return_https_redirects_status` parameter to `1`.")
    customlog: Optional[MainDomainContentsCustomlog] = None
    documentroot: Optional[StrictStr] = Field(default=None, description="The absolute path to the domain's document root.")
    domain: Optional[StrictStr] = Field(default=None, description="The domain's name.")
    group: Optional[StrictStr] = Field(default=None, description="The domain's group.")
    hascgi: Optional[StrictInt] = Field(default=None, description="Whether CGI is enabled for the domain.  * `1` — Enabled. * `0` — Disabled.")
    homedir: Optional[StrictStr] = Field(default=None, description="The absolute path to the cPanel account's home directory.")
    ifmodulemodsuphpc: Optional[MainDomainContentsIfmodulemodsuphpc] = None
    ip: Optional[StrictStr] = Field(default=None, description="The domain's IPv4 address.")
    ipv6: Optional[StrictStr] = Field(default=None, description="The domain's IPv6 address.  * null — The account does **not** use IPv6.")
    is_https_redirecting: Optional[StrictInt] = Field(default=None, description="Whether the domain redirects to `https`.  * `1` — The domain redirects to `https`. * `0` — The domain does **not** redirect to `https`.  **Note:**  This return **only** appears if you set the `return_https_redirects_status` parameter to `1`.")
    no_cache_update: Optional[StrictInt] = Field(default=None, description="Whether the domain is subject to cache updates.  * `1` — The domain updates caches. * `0` — The domain does **not** update caches.")
    owner: Optional[StrictStr] = Field(default=None, description="The WHM account (`root` or a reseller) that owns the cPanel account.")
    scriptalias: Optional[MainDomainContentsScriptalias] = None
    serveradmin: Optional[StrictStr] = Field(default=None, description="The domain's administrator's contact email address.")
    serveralias: Optional[StrictStr] = Field(default=None, description="A space-separated list of the domain's aliases.")
    servername: Optional[StrictStr] = Field(default=None, description="The domain's identifier on the server.")
    type: Optional[StrictStr] = Field(default=None, description="The domain is the cPanel account's main domain.  * `main_domain` is the only possible value.")
    usecanonicalname: Optional[StrictStr] = Field(default=None, description="The domain's Canonical Name (CNAME) setting.  * `On` — Use the domain's CNAME. * `Off` — Do **not** use the domain's CNAME.")
    user: Optional[StrictStr] = Field(default=None, description="The cPanel account's username.")
    userdirprotect: Optional[StrictInt] = Field(default=None, description="The domain's [Apache `mod_userdir` Tweak](https://go.cpanel.net/whmdocsApachemod_userdirTweak) setting.  * `1` — Enabled. * `0` — Disabled.")
    __properties: ClassVar[List[str]] = ["all_aliases_valid", "can_https_redirect", "customlog", "documentroot", "domain", "group", "hascgi", "homedir", "ifmodulemodsuphpc", "ip", "ipv6", "is_https_redirecting", "no_cache_update", "owner", "scriptalias", "serveradmin", "serveralias", "servername", "type", "usecanonicalname", "user", "userdirprotect"]

    @field_validator('all_aliases_valid')
    def all_aliases_valid_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('can_https_redirect')
    def can_https_redirect_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('hascgi')
    def hascgi_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('is_https_redirecting')
    def is_https_redirecting_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('no_cache_update')
    def no_cache_update_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['main_domain']):
            raise ValueError("must be one of enum values ('main_domain')")
        return value

    @field_validator('usecanonicalname')
    def usecanonicalname_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['On', 'Off']):
            raise ValueError("must be one of enum values ('On', 'Off')")
        return value

    @field_validator('userdirprotect')
    def userdirprotect_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MainDomainContents from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of customlog
        if self.customlog:
            _dict['customlog'] = self.customlog.to_dict()
        # override the default output from pydantic by calling `to_dict()` of ifmodulemodsuphpc
        if self.ifmodulemodsuphpc:
            _dict['ifmodulemodsuphpc'] = self.ifmodulemodsuphpc.to_dict()
        # override the default output from pydantic by calling `to_dict()` of scriptalias
        if self.scriptalias:
            _dict['scriptalias'] = self.scriptalias.to_dict()
        # set to None if ipv6 (nullable) is None
        # and model_fields_set contains the field
        if self.ipv6 is None and "ipv6" in self.model_fields_set:
            _dict['ipv6'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MainDomainContents from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "all_aliases_valid": obj.get("all_aliases_valid"),
            "can_https_redirect": obj.get("can_https_redirect"),
            "customlog": MainDomainContentsCustomlog.from_dict(obj["customlog"]) if obj.get("customlog") is not None else None,
            "documentroot": obj.get("documentroot"),
            "domain": obj.get("domain"),
            "group": obj.get("group"),
            "hascgi": obj.get("hascgi"),
            "homedir": obj.get("homedir"),
            "ifmodulemodsuphpc": MainDomainContentsIfmodulemodsuphpc.from_dict(obj["ifmodulemodsuphpc"]) if obj.get("ifmodulemodsuphpc") is not None else None,
            "ip": obj.get("ip"),
            "ipv6": obj.get("ipv6"),
            "is_https_redirecting": obj.get("is_https_redirecting"),
            "no_cache_update": obj.get("no_cache_update"),
            "owner": obj.get("owner"),
            "scriptalias": MainDomainContentsScriptalias.from_dict(obj["scriptalias"]) if obj.get("scriptalias") is not None else None,
            "serveradmin": obj.get("serveradmin"),
            "serveralias": obj.get("serveralias"),
            "servername": obj.get("servername"),
            "type": obj.get("type"),
            "usecanonicalname": obj.get("usecanonicalname"),
            "user": obj.get("user"),
            "userdirprotect": obj.get("userdirprotect")
        })
        return _obj


