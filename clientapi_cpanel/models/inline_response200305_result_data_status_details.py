# coding: utf-8

"""
    cPanel UAPI

    UAPI accesses the cPanel interface's features. Use this API to access and modify cPanel account data and settings.

    The version of the OpenAPI document: 11.125.9901.267
    Contact: cs@cpanel.net
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class InlineResponse200305ResultDataStatusDetails(BaseModel):
    """
    An object that contains specific information about the validation process.
    """ # noqa: E501
    brand_val_status: Optional[StrictStr] = Field(default=None, description="Whether the brand validation status has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The certificate's brand validation is **not** complete. * `completed` - The certificate's brand validation is complete. * `in-progress` - The certificate's brand validation is in progress.", alias="brandValStatus")
    certificate_status: Optional[StrictStr] = Field(default=None, description="Whether the provider has issued the SSL certificate.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The certificate's issue is **not** completed. * `completed` - The provider has issued the certificate. * `in-progress` - The certificate's issue is in progress.", alias="certificateStatus")
    csr_status: Optional[StrictStr] = Field(default=None, description="Whether the certificate signing request (CSR) has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The CSR for the certificate is **not** complete. * `completed` - The CSR for the certificate is complete. * `in-progress` - The CSR for the certificate is in progress.", alias="csrStatus")
    dcv_status: Optional[StrictStr] = Field(default=None, description="Whether Domain Control Validation (DCV) has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The DCV for the certificate is **not** complete. * `completed` - The DCV for the certificate is complete. * `in-progress` - The DCV for the certificate is in progress.", alias="dcvStatus")
    ev_click_through_status: Optional[StrictStr] = Field(default=None, description="Whether every requirement for the EV certificate has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The EV certificate's requirements check is **not** complete. * `completed` - The EV certificate's requirements check is complete. * `in-progress` - The EV certificate's requirements check is in progress.  For more information, read Sectigo's [What is required for validation?](https://support.sectigo.com/Com_KnowledgeDetailPage?Id=kA01N000000zFOr) documentation.", alias="evClickThroughStatus")
    free_dvup_status: Optional[StrictStr] = Field(default=None, description="Whether every requirement for the Domain Validated (DV) certificate has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The DV certificate's requirements check is **not** complete. * `completed` - The DV certificate's requirements check is complete. * `in-progress` - The DV certificate's requirements check is in progress.", alias="freeDVUPStatus")
    organization_validation_status: Optional[StrictStr] = Field(default=None, description="Whether every requirement for the OV certificate has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The OV certificate's requirements check is **not** complete. * `completed` - The OV certificate's requirements check is complete. * `in-progress` - The OV certificate's requirements check is in progress.", alias="organizationValidationStatus")
    ov_callback_status: Optional[StrictStr] = Field(default=None, description="Whether the Certificate Authority (CA) has verified the organization's validity via a phone call.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The CA has **not** verified the organization's validity via phone. * `completed` - The CA has verified the organization's validity via phone. * `in-progress` - The CA's verification of the organization's validity via phone is in progress.", alias="ovCallbackStatus")
    validation_status: Optional[StrictStr] = Field(default=None, description="Whether the validation process has completed.  * `not applicable` - This is **not** applicable for the certificate. * `not-completed` - The certificate's validation is **not** complete. * `completed` - The certificate's validation is complete. * `in-progress` - The certificate's validation is in progress.", alias="validationStatus")
    __properties: ClassVar[List[str]] = ["brandValStatus", "certificateStatus", "csrStatus", "dcvStatus", "evClickThroughStatus", "freeDVUPStatus", "organizationValidationStatus", "ovCallbackStatus", "validationStatus"]

    @field_validator('brand_val_status')
    def brand_val_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('certificate_status')
    def certificate_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('csr_status')
    def csr_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('dcv_status')
    def dcv_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('ev_click_through_status')
    def ev_click_through_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('free_dvup_status')
    def free_dvup_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('organization_validation_status')
    def organization_validation_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('ov_callback_status')
    def ov_callback_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    @field_validator('validation_status')
    def validation_status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['not applicable', 'not-completed', 'completed', 'in-progress']):
            raise ValueError("must be one of enum values ('not applicable', 'not-completed', 'completed', 'in-progress')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InlineResponse200305ResultDataStatusDetails from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InlineResponse200305ResultDataStatusDetails from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "brandValStatus": obj.get("brandValStatus"),
            "certificateStatus": obj.get("certificateStatus"),
            "csrStatus": obj.get("csrStatus"),
            "dcvStatus": obj.get("dcvStatus"),
            "evClickThroughStatus": obj.get("evClickThroughStatus"),
            "freeDVUPStatus": obj.get("freeDVUPStatus"),
            "organizationValidationStatus": obj.get("organizationValidationStatus"),
            "ovCallbackStatus": obj.get("ovCallbackStatus"),
            "validationStatus": obj.get("validationStatus")
        })
        return _obj


