# coding: utf-8

"""
    cPanel UAPI

    UAPI accesses the cPanel interface's features. Use this API to access and modify cPanel account data and settings.

    The version of the OpenAPI document: 11.125.9901.267
    Contact: cs@cpanel.net
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class InlineResponse200423ResultData(BaseModel):
    """
    InlineResponse200423ResultData
    """ # noqa: E501
    cert_match_method: Optional[StrictStr] = Field(default=None, description="The method that the system used to match the certificate with the mail service.  * `none` — No domain matches the certificate. * `exact` — The domain exactly matches the certificate. * `exact-wildcard` — The domain exactly matches the certificate. * `mail-wildcard` — The `mail` subdomain of the domain matches the domain of the wildcard certificate. * `www-wildcard` — The `www` subdomain of the domain matches the domain of the wildcard certificate. * `hostname-wildcard` — The hostname's domain matches the domain of the wildcard certificate. * `hostname` — The hostname matches the domain of the certificate. * `localdomain_on_cert-mail-wildcard` — A `mail` subdomain of a domain on the server that matches the certificate. * `localdomain_on_cert-www-wildcard` — A `www` subdomain of a domain on the server matches the certificate. * `localdomain_on_cert` — A domain on the server matches the certificate.")
    cert_valid_not_after: Optional[StrictInt] = Field(default=None, description="The certificate's expiration date.")
    is_currently_valid: Optional[StrictInt] = Field(default=None, description="Whether the certificate is currently valid.  * `1` — Valid. * `0` — Invalid.  **Note:**  If the certificate is valid, the following statements are true:   * The certificate is **not** self-signed (the `ssldomain_matches_cert` value is `1`).   * The certificate matches the SSL domain (the `ssldomain_matches_cert` value is `1`).   * The certificate is within its validity period (the `cert_valid_not_after` value is **less** than the current time and date).")
    is_self_signed: Optional[StrictInt] = Field(default=None, description="Whether the certificate is self-signed.  * `1` — Self-signed. * `0` — Not self-signed.  **Note:**  If the `cert_match_method` value is `none`, you should ignore this return's value.")
    is_wild_card: Optional[StrictInt] = Field(default=None, description="Whether the certificate is a wildcard certificate.  * `1` — Wildcard. * `0` — Not a wildcard.")
    ssldomain: Optional[StrictStr] = Field(default=None, description="The best domain to use to access the service.")
    ssldomain_matches_cert: Optional[StrictInt] = Field(default=None, description="Whether an SSL-protected domain matches the certificate.  * `1` — Matches. * `0` — Does **not** match.")
    __properties: ClassVar[List[str]] = ["cert_match_method", "cert_valid_not_after", "is_currently_valid", "is_self_signed", "is_wild_card", "ssldomain", "ssldomain_matches_cert"]

    @field_validator('cert_match_method')
    def cert_match_method_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['none', 'exact', 'exact-wildcard', 'mail-wildcard', 'www-wildcard', 'hostname-wildcard', 'hostname', 'localdomain_on_cert-mail-wildcard', 'localdomain_on_cert-www-wildcard', 'localdomain_on_cert']):
            raise ValueError("must be one of enum values ('none', 'exact', 'exact-wildcard', 'mail-wildcard', 'www-wildcard', 'hostname-wildcard', 'hostname', 'localdomain_on_cert-mail-wildcard', 'localdomain_on_cert-www-wildcard', 'localdomain_on_cert')")
        return value

    @field_validator('is_currently_valid')
    def is_currently_valid_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('is_self_signed')
    def is_self_signed_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('is_wild_card')
    def is_wild_card_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    @field_validator('ssldomain_matches_cert')
    def ssldomain_matches_cert_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set([1, 0]):
            raise ValueError("must be one of enum values (1, 0)")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of InlineResponse200423ResultData from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of InlineResponse200423ResultData from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "cert_match_method": obj.get("cert_match_method"),
            "cert_valid_not_after": obj.get("cert_valid_not_after"),
            "is_currently_valid": obj.get("is_currently_valid"),
            "is_self_signed": obj.get("is_self_signed"),
            "is_wild_card": obj.get("is_wild_card"),
            "ssldomain": obj.get("ssldomain"),
            "ssldomain_matches_cert": obj.get("ssldomain_matches_cert")
        })
        return _obj


